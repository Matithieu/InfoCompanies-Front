import { defineMessages } from 'react-intl'

const scope = 'page.favorites'

export default defineMessages({
  noCompanyInToDo: {
    defaultMessage: 'No company in To Do!',
    id: `${scope}.noCompanyInToDo`,
  },
})
