import { defineMessages } from 'react-intl'

const scope = 'page.search'

export default defineMessages({
  companyFor: {
    defaultMessage: 'Companies for: {searchValue}',
    id: `${scope}.companyFor`,
  },
})
